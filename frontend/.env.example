PORT=3000

# Base path the SPA is served under behind a reverse proxy (e.g. /cbt).
# Leave empty for local development.
BASE_PATH=

# Where the browser sends API calls. Defaults to the backend origin in dev, or
# "<BASE_PATH>/api" (same-origin) when BASE_PATH is set.
API_BASE=http://localhost:5000/api
